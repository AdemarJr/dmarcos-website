import { ConsultasLayout } from "@/components/consultas/ConsultasLayout"

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ConsultasLayout>{children}</ConsultasLayout>
}

